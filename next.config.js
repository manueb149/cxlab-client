/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'tailwindui.com',
      },
      {
        protocol: 'https',
        hostname: 'generation-sessions.s3.amazonaws.com',
      },
    ],
  },
}

module.exports = nextConfig
